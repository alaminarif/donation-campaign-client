import React from "react";

const HeroSection = () => {
  return (
    <div
      style={{
        minHeight: "calc(100vh - 70px )",
        backgroundColor: "#ddd",
      }}
    >
      this is hero section
    </div>
  );
};

export default HeroSection;
