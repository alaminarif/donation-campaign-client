const page = () => {
  return (
    <div
      style={{
        minHeight: "calc(100vh - 70px )",
        // backgroundColor: "#ddd",
      }}
    >
      this is about page
    </div>
  );
};

export default page;
